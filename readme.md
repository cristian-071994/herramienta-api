en el controlador del usuario creamos las funciones que tengan que ver con el usuario
-- registerUser
-- loginUser
* tomamos las credenciales y comparamos la contraseña si es correcta
*creamos el token
